# winshorturl
A simple script that creates shortlink asp files

To use place makeURL.vbs into the folder you want shortlinks like C:\inetpub\wwwroot\go.

Then to create a shortlink just open makeURL.vbs
